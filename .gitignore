# Build outputs
bin/
obj/

# IDE
.vs/
*.user
*.suo

# Debug
*.log
integrated_mods/

# Local backup copies
ExtendedLSC-backup-*/
ExtendedLSC_backup_/

# Native ASI build artifacts (ASI is unused; source kept for reference)
native/**/build/
native/ScriptHookV_SDK/

# Runtime backup of deployed game-dir save data (local only)
GameDataBackup/

# DLC build intermediates (the packaged dlc_elsc/ IS committed)
DLC/_build/

# Editor / process temp files
*.tmp.*

# Python tooling caches + dyno run outputs
__pycache__/
tools/dyno/results/

# Speedo skin local backups
speedo/*_backup/
*_orig_backup/

# Release package + page assets (built artifacts, not source)
- Upload -/
pictures/

# Enhanced glass-DLC built binary (the xml shell IS committed; the ytd/rpf is regenerated)
DLC/dlc_elsc_enhanced/x64/

# dyno watch-loop state files
*_watch.txt
